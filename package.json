{
  "name": "reg-form-components",
  "version": "3.0.0",
  "private": true,
  "description": "Runtime-loaded section components for the registration form.",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "node build.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
